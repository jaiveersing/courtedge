# CourtEdge ML Service - Render Deployment
# https://render.com - FREE tier available

# Build Command
build:
  - pip install -r requirements.txt

# Start Command
start: uvicorn src.api:app --host 0.0.0.0 --port $PORT

# Environment
python: "3.11"
